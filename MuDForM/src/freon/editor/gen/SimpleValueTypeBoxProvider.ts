// Generated by the Freon Language Generator.
import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory, FreNodeReference, LimitedDisplay } from "@freon4dsl/core";

import { SimpleValueType, DatomicType } from "../../language/gen/index.js";

import { MuDForM_ModelEnvironment } from "../../config/gen/MuDForM_ModelEnvironment.js";

/**
 * This class implements the box provider for a single node of type SimpleValueType.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class SimpleValueTypeBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["default"];
        this.knownTableProjections = ["default"];
        this.conceptName = "SimpleValueType";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "default") {
                return this.getDefault();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(this._node as SimpleValueType, "SimpleValueType-overall", "", [
            BoxFactory.horizontalLayout(
                this._node as SimpleValueType,
                "SimpleValueType-hlist-line-0",
                "",
                [
                    BoxUtil.labelBox(this._node as SimpleValueType, "SimpleValueType", "top-1-line-0-item-0"),
                    BoxUtil.labelBox(this._node as SimpleValueType, "\{", "top-1-line-0-item-1"),
                ],
                { selectable: false },
            ),
            BoxUtil.indentBox(
                this._node as SimpleValueType,
                4,
                "1",
                BoxFactory.horizontalLayout(
                    this._node as SimpleValueType,
                    "SimpleValueType-hlist-line-1",
                    "",
                    [
                        BoxUtil.labelBox(this._node as SimpleValueType, "primitive_type", "top-1-line-1-item-0"),
                        BoxUtil.limitedBox(
                            this._node as SimpleValueType,
                            "primitive_type",
                            (selected: string) => {
                                (this._node as SimpleValueType).primitive_type = FreNodeReference.create<DatomicType>(
                                    selected,
                                    "DatomicType",
                                );
                            },
                            LimitedDisplay.SELECT,
                            MuDForM_ModelEnvironment.getInstance().scoper,
                        ),
                    ],
                    { selectable: false },
                ),
            ),
            BoxUtil.labelBox(this._node as SimpleValueType, "}", "top-1-line-2-item-0"),
        ]);
    }
}
