// Generated by the Freon Language Generator.
import { MobxModelElementImpl, observableprim, FreNamedNode, FreParseLocation, FreUtils } from "@freon4dsl/core";

import { runInAction, makeObservable, action } from "mobx";
/**
 * Class DatomicType is the implementation of the limited concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class DatomicType extends MobxModelElementImpl implements FreNamedNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<DatomicType>): DatomicType {
        const result = new DatomicType(data.$id);
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    static bigdec: DatomicType; // implementation of instance bigdec
    static bigint: DatomicType; // implementation of instance bigint
    static boolean: DatomicType; // implementation of instance boolean
    static bytes: DatomicType; // implementation of instance bytes
    static double: DatomicType; // implementation of instance double
    static float: DatomicType; // implementation of instance float
    static instant: DatomicType; // implementation of instance instant
    static keyword: DatomicType; // implementation of instance keyword
    static long: DatomicType; // implementation of instance long
    static string: DatomicType; // implementation of instance string
    static symbol: DatomicType; // implementation of instance symbol
    static uuid: DatomicType; // implementation of instance uuid
    static uri: DatomicType; // implementation of instance uri
    static $freANY: DatomicType; // default predefined instance

    readonly $typename: string = "DatomicType"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    name: string; // implementation of name

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "";

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): DatomicType {
        const result = new DatomicType();
        if (!!this.name) {
            result.name = this.name;
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<DatomicType>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        return result;
    }
}

// Because of mobx we need to generate the initialisations outside of the class,
// otherwise the state of properties with primitive type will not be kept correctly.
runInAction(() => {
    DatomicType.bigdec = DatomicType.create({
        name: "bigdec",
    });
    DatomicType.bigint = DatomicType.create({
        name: "bigint",
    });
    DatomicType.boolean = DatomicType.create({
        name: "boolean",
    });
    DatomicType.bytes = DatomicType.create({
        name: "bytes",
    });
    DatomicType.double = DatomicType.create({
        name: "double",
    });
    DatomicType.float = DatomicType.create({
        name: "float",
    });
    DatomicType.instant = DatomicType.create({
        name: "instant",
    });
    DatomicType.keyword = DatomicType.create({
        name: "keyword",
    });
    DatomicType.long = DatomicType.create({
        name: "long",
    });
    DatomicType.string = DatomicType.create({
        name: "string",
    });
    DatomicType.symbol = DatomicType.create({
        name: "symbol",
    });
    DatomicType.uuid = DatomicType.create({
        name: "uuid",
    });
    DatomicType.uri = DatomicType.create({
        name: "uri",
    });
});
