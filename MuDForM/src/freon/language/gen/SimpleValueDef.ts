// Generated by the Freon Language Generator.
import { observableprim, observablepart, FreNode, FreParseLocation, FreNodeReference } from "@freon4dsl/core";
import { ValueDef, DbType } from "./internal.js";

/**
 * Class SimpleValueDef is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class SimpleValueDef extends ValueDef implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<SimpleValueDef>): SimpleValueDef {
        const result = new SimpleValueDef(data.$id);
        if (!!data.doc) {
            result.doc = data.doc;
        }
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.db_type) {
            result.db_type = data.db_type;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "SimpleValueDef"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    doc: string; // implementation of doc

    db_type: FreNodeReference<DbType>; // implementation of reference 'db_type'

    constructor(id?: string) {
        super(id);
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "doc");
        this.doc = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "db_type");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): SimpleValueDef {
        const result = new SimpleValueDef();
        if (!!this.doc) {
            result.doc = this.doc;
        }
        if (!!this.name) {
            result.name = this.name;
        }
        if (!!this.db_type) {
            result.db_type = this.db_type.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<SimpleValueDef>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && toBeMatched.doc !== null && toBeMatched.doc !== undefined && toBeMatched.doc.length > 0) {
            result = result && this.doc === toBeMatched.doc;
        }
        if (result && !!toBeMatched.db_type) {
            result = result && this.db_type.match(toBeMatched.db_type);
        }
        return result;
    }

    /**
     * Convenience method for reference 'db_type'.
     * Instead of returning a 'FreNodeReference<DbType>' object,
     * it returns the referred 'DbType' object, if it can be found.
     */
    get $db_type(): DbType {
        if (!!this.db_type) {
            return this.db_type.referred;
        }
        return null;
    }
}
